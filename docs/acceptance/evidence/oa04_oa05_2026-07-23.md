# OA-04 / OA-05 open-alpha acceptance evidence

Date: 2026-07-23 (America/Chicago)  
Result: **OA-04 pass; OA-05 pass**  
Sunlight: `sun 0.1.0`, source commit `7a7ed0dbaa0ac41d06248ea0bde2c40284faf098` plus the tested uncommitted worktree  
Tested source identity: tracked `crates/` diff digest `9e41fb775970b6ae69676a17473a6402d9a14c6c`; new `open_alpha_handoff.rs` Git blob `597d8da9aeee2b47f0d0dd186ca927fc027f0392`  
Host: Microsoft Windows 10.0.26200.8875, NTFS, Rust 1.95.0, Cargo 1.95.0, Git 2.52.0.windows.1, Python 3.11.15  
Harness/model: Codex desktop, GPT-5; reasoning effort was not exposed in the delegation metadata.

## Exact delegated prompt

> Work as the second engineer in a tag-team effort to satisfy every locally automatable requirement in docs/open_alpha_acceptance.md.
>
> Your owned workstream is OA-04 (MCP termination and crash recovery) and OA-05 (exact validation and Git compatibility handoff). Work directly in the current Sunlight 2 project, using isolated temporary/sacrificial repositories with no production remotes. Inspect existing implementation and tests first; implement missing behavior, add durable automated acceptance coverage, run the relevant focused and workspace verification, and create a concise evidence document for these two gates. Fix product defects you find when the correct behavior is clear.
>
> Do not edit docs/open_alpha_acceptance.md or README.md; the coordinating task will update the master acceptance record after reviewing your work. Avoid unrelated refactors. Do not commit, push, add a remote, or modify production repositories. Follow AGENTS.md, including use of the alternative-patch skill for file edits.
>
> Keep going through both gates unless a genuinely external user action is required. Report exact files changed, commands/results, what is fully proven versus still partial, and any blockers.

The final coordination instruction was to run the Python OA-05 case outside the Windows low-integrity sandbox when required, run full `mcp_stdio` coverage, write this record, remove only `.tmp-oa-debug`, and not broaden scope.

## Targets and safety

Both tests create disposable repositories under the user's NTFS `%TEMP%` directory and delete them after the assertions. OA-04 starts clean with one 17-byte tracked file. OA-05 starts clean with four tracked files totaling 264 bytes. Both assert an empty `git remote` list. No production remote was configured, no push occurred, and no commit was created in the Sunlight source repository. Commits and branches mentioned below exist only in the sacrificial OA-05 repository.

The acceptance harness seeds fixtures and independently reads Git/state for verification. During the authored workflow, application reads and mutations go through Sunlight; direct filesystem/Git access is limited to fixture setup, fault injection, and independent acceptance assertions.

## OA-04: MCP termination and crash recovery

Automated case: `open_alpha_oa04_mcp_termination_boundaries_recover_from_durable_facts` in `crates/sun/tests/mcp_stdio.rs`.

Exact retained objects from the final run:

- topic `topic_oa04_recovery`; session `session_oa04_agent`
- operation/revision `rev_oa04_recovery_0001`
- exact view `view_fixture_bc7f925c47f7d4cb`
- execution `exec_native_0001`; execution projection `projection_execution_copy_native_0001`
- checkpoint `checkpoint_c1f7f12211ead374`
- changed path `src/recovered.txt`, bytes `acknowledged before termination\n`, SHA-256 `50da58c47325f1f3a9557834214c741192eb4ad1d6eb188b9df20f9d4425c0fe`

The test force-terminates and restarts the MCP server at all five required boundaries: immediately after an acknowledged mutation; while a 30-second Python execution is running; after topic completion; while an independently held OS writer lease blocks repository access; and immediately before checkpoint creation. Recovery uses persisted repository facts without deleting `.sunlight`. The acknowledged artifact remains exact and singular, the completed revision remains readable, the writer lease clears when its process/handle ends, and the checkpoint binds the recovered exact view. The running record is durable before server loss and recovers to `interrupted` with termination reason `runner_process_terminated`.

No conflict or staleness occurred. The writer-lock case required no manual lock deletion or retry loop. The intentionally interrupted execution is separately followed by an exact checkpoint/read assertion.

Final focused result:

```text
$ cargo test -p sun --test mcp_stdio open_alpha_oa04_mcp_termination_boundaries_recover_from_durable_facts -- --nocapture
running 1 test
OA-04 evidence {"checkpoint_id":"checkpoint_c1f7f12211ead374","execution_id":"exec_native_0001","recovered_status":"interrupted","revision_id":"rev_oa04_recovery_0001","session_id":"session_oa04_agent","topic_id":"topic_oa04_recovery","view_id":"view_fixture_bc7f925c47f7d4cb"}
test ... ok
test result: ok. 1 passed; 0 failed; 6 filtered out; finished in 1.32s
```

Full transport regression result, independently rerun by the coordinator on the shared tree: `cargo test -p sun --test mcp_stdio -- --nocapture` -> **7 passed, 0 failed**.

## OA-05: exact validation and Git compatibility handoff

Automated case: `open_alpha_oa05_exact_checkpoint_exports_to_safe_buildable_git_handoff` in `crates/sun/tests/open_alpha_handoff.rs`.

Exact retained objects from the final run:

- sacrificial base commit `aad9862d428c0ed6c463a5de7ce8183a86e9367e`
- topic `topic_fix_answer`; session `session_oa05_agent`; revision `rev_fix_answer_0001`
- exact view `view_fixture_06eba2a8df15ccb1`
- red/focused/full executions `exec_native_0001`, `exec_native_0002`, `exec_native_0003`
- execution projections `projection_execution_copy_native_0001` through `projection_execution_copy_native_0003`
- checkpoint `checkpoint_bc8f400950df641a_66ae1142476e`
- tree `tree_bc8f400950df641adf0102bb94fa23e9a394304e0701ebd02011600d04983d8c` in repository `repo-01e45590c66f0a3d`
- export map `export_map_checkpoint_bc8f400950df641a_66ae1142476e`
- branch `refs/heads/sunlight/oa05-handoff`; exported commit `479078085d9c43a8d934494021c0531f7eaf868a`
- changed path `app.py`: failing SHA-256 `bb90d2b315ed538549cd708303971dd03d30dd733928b845504fab3a42eeb914`, passing SHA-256 `aecc013c518523ca22b7e3780f87002f5cae13fabc369f2f59a1faf7058f8c76`

The imported base's `python -m unittest -v` is durably recorded as `fail`. After the Sunlight-authored `41` to `42` change, the same exact view records `python -m unittest -v` and the full-validation equivalent `python -m compileall -q .` as `pass`. The checkpoint evidence references the full-validation execution and its exact view/tree.

Before export, the harness creates an unstaged dirty `README.md`, staged `dirty-staged.txt`, and ignored `__pycache__/ignored-leak.pyc`. Export leaves that user status and README bytes unchanged. The exported path list exactly equals every non-tombstone checkpoint entry, and `git show <commit>:<path>` is byte-equal for every entry. Dirty, ignored, and `.sunlight` paths do not leak. A pre-existing unrelated collision ref is rejected with `export_target_ref_conflict` and remains unchanged. Retrying the same checkpoint/ref returns the same commit and leaves exactly one durable export mapping. An ordinary Git diff shows `return 41` to `return 42`; a detached Git-only worktree passes both unittest and compileall without Sunlight. No remote exists.

Final focused result (run outside the managed low-integrity sandbox because the Windows projection ACL setup otherwise returns OS error 5):

```text
$ cargo test -p sun --test open_alpha_handoff -- --nocapture
running 1 test
OA-05 evidence {"base_commit":"aad9862d428c0ed6c463a5de7ce8183a86e9367e","branch":"refs/heads/sunlight/oa05-handoff","checkpoint_id":"checkpoint_bc8f400950df641a_66ae1142476e","consumer_validation":"pass","export_map_id":"export_map_checkpoint_bc8f400950df641a_66ae1142476e","exported_commit":"479078085d9c43a8d934494021c0531f7eaf868a","failing_execution_id":"exec_native_0001","focused_execution_id":"exec_native_0002","full_execution_id":"exec_native_0003","tree_identity":{"kind":"SingleRepoTree","repository_id":"repo-01e45590c66f0a3d","tree_hash":"tree_bc8f400950df641adf0102bb94fa23e9a394304e0701ebd02011600d04983d8c"},"view_id":"view_fixture_06eba2a8df15ccb1"}
test ... ok
test result: ok. 1 passed; 0 failed; finished in 7.08s
```

## Supporting verification and findings

- `cargo test -p sunlight-core git_export::tests -- --nocapture` -> **46 passed, 0 failed**.
- `cargo test -p sunlight-core repo_state::tests -- --nocapture` -> **26 passed, 0 failed**.
- `cargo check --workspace` -> finished successfully.
- Independent final shared-tree verification: sunlight-core **184/184**, `cli_json` **229/229**, `mcp_stdio` **7/7**, and `open_alpha_handoff` **1/1**.

Defects fixed and covered: durable two-phase running/final execution publication and dead-runner recovery; accurate Windows process liveness; Windows atomic replacement without failing ACL metadata merge; idempotent same-checkpoint/same-ref Git export retry; and exact checkpoint-to-Git byte verification. The previously observed deterministic `ReplaceFileW` access-denied regression is fixed; full MCP integration coverage passes.

Timing recorded here is harness wall time only (OA-04 1.32s, OA-05 7.08s); scale latency, cache, and storage amplification are outside these two gates. Execution responses retain their normal projection/materialization and policy facts. There are no OA-04/OA-05 blockers or partial criteria in the declared Windows/NTFS scope.

The source workspace remained intentionally dirty for the coordinated implementation and contains concurrent workstreams. This acceptance work did not stage, commit, add a remote, or push anything in the source repository.

## Final release revalidation

The final release executable is SHA-256 `d69c6cb6ddd6e75f76491ab040a6fb3ec723249831635a610157e58cb1de10b8`. On the final source tree, the authoritative unsandboxed `cargo test -p sun` run passed unit **29/29**, CLI **231/231**, engine **2/2**, MCP **7/7** (including OA-04), exact Git handoff **1/1** (OA-05), and self-hosting **1/1**. Sunlight-core independently passed **188/188**. The managed-sandbox run's three AppContainer-profile denials were host test-runner restrictions; the exact unsandboxed rerun passed those containment tests and the complete suite.
