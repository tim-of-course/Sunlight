# OA-08 Windows-only platform acceptance evidence

Date: 2026-07-24 (America/Chicago)  
Result: **OA-08 pass**  
Declared open-alpha scope: **Windows only**, tested on Windows/NTFS  
Build: `d69c6cb6ddd6e75f76491ab040a6fb3ec723249831635a610157e58cb1de10b8`

## Decision and public contract

The product owner selected a Windows-only open alpha. The README now states
that scope before build or installation instructions. It does not describe
macOS or Linux compilation, best-effort cleanup, or weaker policy reporting as
supported behavior or enforced isolation.

## Lifecycle proof in the supported scope

The retained final-build evidence bundle covers the required Windows lifecycle:

- installation, repository initialization, generated Codex/Cursor bindings,
  doctor output, and machine-local configuration contracts:
  `oa01_oa09_automated_2026-07-23.md`;
- initialization, exact authoring, common-path integration, execution, recovery,
  checkpointing, and structured error recovery: `oa02_oa06_2026-07-23.md`;
- MCP restart recovery at all required publication boundaries and exact Git
  export into an ordinary Git-only consumer: `oa04_oa05_2026-07-23.md`;
- four-author Windows/NTFS scale, projection, cache, storage, checkpoint, and
  final-build behavior: `oa07_2026-07-23.json`.

The exact release executable and final source suites were revalidated as
recorded in those files. No evidence file claims a supported non-Windows run.

## Containment statement

Windows is the only supported alpha platform and the only platform on which the
full acceptance contract is asserted. Known limits remain visible in execution
policy reports; in particular, Sunlight does not turn an unenforced dimension
into an isolation claim. Non-Windows code paths may compile or report
best-effort behavior, but they are outside the published support contract.

## Safety

The declaration and evidence update changed documentation only. It created no
commit, remote, or push and did not alter any target repository.
