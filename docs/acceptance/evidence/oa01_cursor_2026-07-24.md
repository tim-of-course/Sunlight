# OA-01 Cursor agent acceptance evidence

Date: 2026-07-24 (America/Chicago)  
Build SHA-256: `d69c6cb6ddd6e75f76491ab040a6fb3ec723249831635a610157e58cb1de10b8`  
Host: Windows NT 10.0.26200, Windows/NTFS supported scope  
Client: Fresh Cursor Agent task using the repository-installed Cursor adapter  
Target: Disposable CheckChecker repository, no Git remote

Result: **OA-01 pass**.

## Setup and prompt

Before the run, prior native state was removed from the disposable repository.
`sun agent install --client cursor` installed `.cursor/mcp.json`, and
`sun agent doctor --client cursor` reported the adapter healthy and current,
required a client restart, and accurately reported that the repository was not
initialized. Cursor was reloaded and a fresh Agent task received only this
natural engineering prompt:

> Use Sunlight to add a reusable helper that identifies queued sync work that
> has exhausted its retry budget and display that count in the offline checklist
> notice. Keep it lean, validate the exact result, and do not commit, export, add
> remotes, or push. If Sunlight is unavailable, stop and tell me the setup action
> instead of editing source directly.

No Sunlight object IDs, MCP tool names, or lifecycle sequence were supplied.

## Persisted result

The Cursor agent discovered and used Sunlight without workflow coaching. It
created and completed `topic_exhausted_retry_sync_notice` through
`rev_exhausted_retry_sync_notice_0004` in
`session_cursor_agent_exhausted_retry`. The immutable handoff records four
native operations changing only:

- `src/lib/syncQueue.ts`
- `src/components/FieldChecklist.tsx`

The exact resolved view `view_fixture_d11fb9ad3f884e88` has no conflicts or
staleness and resolves to tree
`tree_3fd753e699742c75bb23a25680ca46f58242f3bfa5f2454f6a45990cdb92e44e`.

Persisted validation on that same view:

- `exec_native_0001`: `bun run build`, pass, exit code 0.
- `exec_native_0002`: focused helper assertion, pass, exit code 0; observed
  output digest corresponds to the reported
  `{"ok":true,"exhaustedCount":2,"budget":5}` result.
- `checkpoint_3fd753e699742c75_3e294e4fed05`: unexported checkpoint over the
  exact validated view and tree, with `exec_native_0001` bound as evidence.

Repository status reports two passed executions, zero failed executions, one
completed topic, no conflicts, no staleness, one unexported checkpoint, and no
exports. Git has no remote and no tracked working-tree diff; only `.agents/`,
`.codex/`, `.cursor/`, and `.sunlight/` are untracked. No commit, export, remote,
or push was created, and the agent reported no tracked-source access outside
Sunlight.

## Non-blocking observations

- Repository status emits `git_working_tree_changed` for the expected untracked
  agent/client/state directories even though tracked source is unchanged. The
  warning is conservative but imprecise.
- Cursor completed the topic before validation, so the immutable handoff summary
  says validation was pending. Both later executions and the checkpoint bind to
  the same exact view and tree, so the acceptance evidence remains complete.
  Agent guidance should continue to prefer validate-before-complete when the
  final handoff summary should state validation status.

## Classification

The required fresh Cursor run discovered the repository adapter, performed real
engineering work through Sunlight, validated the exact result, and left Git
source and delivery state untouched. Combined with the three consecutive fresh
Codex passes, OA-01 passes.
